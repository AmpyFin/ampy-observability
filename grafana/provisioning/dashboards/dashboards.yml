apiVersion: 1

providers:
  - name: "ampy dashboards"
    orgId: 1
    folder: "Ampyfin"
    type: file
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
